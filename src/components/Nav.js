import React from 'react';

const Nav = (props) => {
  return (
    <div style={styles.nav}>
      <p>Navagation</p>
    </div>
  );
};
export default Nav;

const styles = {
  nav: {
    backgroundColor: 'green',
  },
};
